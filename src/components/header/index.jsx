const Header1 = (props) => {

    return (
        <h1>{props.header}</h1>
    )
}

const Header3 = (props) => {

    return (
        <h3>{props.header}</h3>
    )
}

export default Header3;
export { Header1 };